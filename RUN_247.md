# دليل التشغيل المستمر 24/7 — راشد الاستخباراتي v2.1

هذا الدليل يشرح كيف تجعل بوتك يعمل باستمرار دون توقف، بعد الإصلاحات التي أُجريت على الكود.

## المشكلة السابقة وأسبابها

كان البوت يتوقف عن العمل لأسباب متعددة تراكمت مع بعضها:

1. **استضافة Replit المجانية** توقف العمليات الخاملة (polling) بعد فترة قصيرة من الخمول، وعناوينها (`pike.replit.dev`) تتغير مع كل تشغيل.
2. **لا يوجد نظام إعادة تشغيل تلقائي** — عندما كان البوت يسقط لأي سبب (خطأ غير متوقع، انقطاع شبكة)، لا أحد يعيد تشغيله.
3. **مفتاح Groq API منتهي** (خطأ 401) — هذا سبب رسالة "خطأ في الذكاء الاصطناعي: 401".
4. **مفتاح Tavily تجريبي** (`tvly-dev-...`) محدود وقد يتوقف فجأة.

## الحلول المطبقة في الكود

| الإصلاح | الوصف |
|---------|-------|
| حلقة تشغيل مستقرة | `run_polling` داخل حلقة `while` — أي انقطاع يعيد الاتصال تلقائياً بعد 10 ثوانٍ |
| معالج أخطاء مركزي | أي استثناء غير متوقع يُسجَّل ولا يوقف البوت |
| مراقب صحة ذاتي | فحص كل 5 دقائق؛ إذا واجهة تلغرام لا تستجيب يوقف البوت ليعاد تشغيله تلقائياً |
| تسجيل الأخطاء | كل الأخطاء تُحفظ في `bot_error.log` لمراجعتها |
| رسائل خطأ واضحة | المستخدم يرى رسالة مفهومة بدلاً من كود خطأ خام |

## الخيار 1: تشغيل على سيرفر VPS (الأفضل — موصى به)

### على أي سيرفر Linux (Ubuntu/Debian) يعمل 24/7

**الخطوة 1:** ارفع ملفات البوت إلى السيرفر (عبر git clone أو SFTP).

**الخطوة 2:** أنشئ ملف `.env` في مجلد البوت بالمفاتيح الخاصة بك (انسخ `.env.example` وعدّله).

**الخطوة 3:** ثبّت المكتبات:

```bash
pip3 install -r requirements.txt
```

**الخطوة 4:** ثبّت خدمة systemd (تشغيل تلقائي + إعادة تشغيل عند السقوط):

```bash
chmod +x systemd/install_service.sh
sudo ./systemd/install_service.sh
```

بعد التثبيت البوت يعمل تلقائياً حتى بعد إعادة تشغيل السيرفر، ويُعاد تشغيله خلال 15 ثانية عند أي سقوط.

**أوامر الإدارة:**

```bash
sudo systemctl status rashd-bot    # حالة البوت
sudo journalctl -u rashd-bot -f    # متابعة السجل
sudo systemctl restart rashd-bot   # إعادة تشغيل
sudo systemctl stop rashd-bot      # إيقاف
```

سيرفرات VPS رخيصة (2–5 دولار/شهر): Hetzner، DigitalOcean، Contabo، Vultr.

## الخيار 2: تشغيل على Render.com (مجاني جزئياً)

استخدم `RENDER_SETUP.md` الموجود في المستودع، مع ملاحظة مهمة:

> الخطة المجانية في Render **توقف الخدمة بعد 15 دقيقة من عدم النشاط**. لتجنب ذلك فعّل خيار "Auto-Deploy" واستخدم خدمة مراقبة مثل UptimeRobot لإرسال طلب كل 5 دقائق إلى `https://رابطك/on_render_ping` (توجد نقطة `/ping` في `tracker_server.py`).

## الخيار 3: Replit (غير موصى به)

Replit المجاني يوقف البوت باستمرار. إن كنت مصراً على استخدامه، فعّل "Always On" (مدفوع) أو استخدم UptimeRobot على رابط الخدمة.

## مفاتيح API — حالة كل مفتاح

| المفتاح | الحالة | الإجراء المطلوب |
|---------|--------|-----------------|
| `MAIN_BOT_TOKEN` | ✅ صالح | لا شيء |
| `TRACKER_BOT_TOKEN` | ✅ صالح | لا شيء |
| `GROQ_API_KEY` | ❌ منتهي (401) | استبدله بمفتاح جديد من https://console.groq.com |
| `TAVILY_API_KEY` | ⚠️ تجريبي | يستجيب لكن محدود — يُفضل الترقية لـ production key |
| `IPINFO_TOKEN` | ✅ صالح | لا شيء |
| `VIRUSTOTAL_API_KEY` | لم يُختبر | تحقق من https://www.virustotal.com/gui/my-api-key |
| `LEAKCHECK_KEY` | لم يُختبر | تحقق من https://leakcheck.io |
| `BOT_SERVER_URL` | ❌ رابط Replit قديم | حدّثه بعد النقل إلى استضافة جديدة |

## بعد استبدال مفتاح Groq

1. احصل على مفتاح جديد مجاني من https://console.groq.com (منحهم 1000 طلب/دقيقة للخطة المجانية).
2. عدّل `GROQ_API_KEY` في `.env`.
3. أعد تشغيل البوت: `sudo systemctl restart rashd-bot`

## التحقق من عمل البوت

```bash
# اختبار الاستجابة
curl -s "https://api.telegram.org/bot<MAIN_BOT_TOKEN>/getWebhookInfo"

# متابعة الأخطاء لحظياً
tail -f bot_error.log
```

**تاريخ آخر تحديث:** 2026-08-12
